{% extends 'base.html.twig' %}

{% block title %}Edit Stock{% endblock %}

{% block body %}
<div class="container">
    <div class="row justify-content-center">
        <div class="col-lg-8">
            <div class="card">
                <div class="card-header">
                    <h2 class="text-center">Edit Stock</h2>
                </div>
                <div class="card-body">
                    {{ include('stock/_form.html.twig', {'button_label': 'Update'}) }}
                </div>
                <div class="card-footer">
                    <a href="{{ path('app_stock_back') }}" class="btn btn-secondary">Back to List</a>
                </div>
            </div>
            <div class="mt-3">
                {{ include('stock/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
</div>
{% endblock %}
